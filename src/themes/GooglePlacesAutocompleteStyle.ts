import { Colors } from 'react-native/Libraries/NewAppScreen';
import { Fonts } from '.';

const style = {
  container: {
    width: '100%',
    pointerEvents: 'auto',
    zIndex: 10,
    top: 40,
    left: 15,
    position: 'absolute',
  },
  textInputContainer: {},

  poweredContainer: {},
  listView: {
    pointerEvents: 'auto',
    zIndex: 10,
  },
  row: {
    zIndex: 10,
    pointerEvents: 'auto',
  },
};

export default style;
